{% extends 'base.html.twig' %}

{% block title %}Edit Comment{% endblock %}

{% block body %}
    <h1>Edit Comment</h1>

    {{ form_start(form) }}
        {{ form_row(form.content) }}
        <button type="submit" class="btn btn-primary">Save Changes</button>
    {{ form_end(form) }}

    <a href="{{ path('comment_index', {'vehicleId': vehicle.id}) }}" class="btn btn-secondary">Back to comments</a>
{% endblock %}
